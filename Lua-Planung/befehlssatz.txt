  trans(input string)

Befehle:
 
  send_file(name, type)
  receive_file(checksum, name)
  get_file(name, type)
  
  send_com(comm_id, prog_id, args)
  receive_com(comm_id, prog_id, args)
  
  connect(ip, port)
  disconnect()
  auth()
  
 
Syntax:
  send ip (file|command) (file_name="string"|command_name="string") [type="string"|args="string[]"] [port"int"]
